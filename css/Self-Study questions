If you were to describe semantic HTML to the next cohort of students, what would you say?
- Semantic HTML or semantic markup is HTML that introduces meaning to the web page rather than just presentation.


Name two big differences between display: block; and display: inline;.
- display:inline : adjacent elements only react to horizontal padding and margin.
- dispaly:block : adjacent elements can react horizontally and vertically with padding and margin, also its default width is the width of the container, forces a line break, and respect height & width when specified.


What are the 4 areas of the box model?
- the 4 areas of the box model are Margin, Border, padding, content.


While using flexbox, what axis does the following property work on: align-items: center?
- align-items: center; works on the cross axis.


Explain why git is valuable to a team of developers.
- Git is valuable to a team of developers because it allows multiple people work on projects without being physically there and also allow them to check what other developers have edited and created.
